<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="CleanProject.Views.CornerListPage">
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Locations}" Margin="20"  SelectedItem="{Binding SelectedLocation, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="20,0,0,0" Orientation="Horizontal">
                            <Image HorizontalOptions="CenterAndExpand" 
                                   Source="{Binding LocationUrl}"
                                   WidthRequest="200"
                                   HeightRequest="200"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
  
</ContentPage>